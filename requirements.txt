# Rdzeń
httpx>=0.27,<0.29
pydantic>=2.6,<3
python-dotenv>=1.0

# CLI (Etap 2)
typer>=0.12

# Backend (Etap 3)
fastapi>=0.110
uvicorn[standard]>=0.29
openpyxl>=3.1          # eksport XLSX

# Dev / testy
pytest>=8.0
respx>=0.21            # mockowanie httpx bez sieci
